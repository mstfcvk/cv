html  than enter will boilerplate will automatically
if blue dot, should save the file

<meta charset="utf-8"/> means all code in the page is encoded in utf characters
utf-8 standard encoding, should always use it

packages -> atop-beautify will automatically indent all lines/codes

hyperText markup Lnaguage hyperText= bunch of text that directs to another text

html for structure only


forms
<br>
<label for="">input text</label>
<input type="text"><br>
<label for="">input submit</label>
<input type="submit"><br>
<label >input button</label>
<input type="<button"><br>

input types: button date radio, chode a file ...

at video of 27
